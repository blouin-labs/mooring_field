# CLAUDE.md

Do not edit this file. The actual instructions for this repo are stored privately in `blouin-labs/claude`.

At the start of every session, run the following commands and read their output before proceeding with any work:

```bash
gh api repos/blouin-labs/claude/contents/CLAUDE.md --jq '.content' | base64 -d
gh api repos/blouin-labs/claude/contents/mooring_field/CLAUDE.md --jq '.content' | base64 -d
```

## Issues

Issues for this repo live in **[blouin-labs/issues](https://github.com/blouin-labs/issues)**. Apply the `mooring_field` label to issues related to this repo.
